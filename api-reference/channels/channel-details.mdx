---
title: "Channel Details"
api: "GET /api/channels/{channelId}/info"
description: "Get detailed information and statistics for a specific channel"
---

## Overview

Retrieve comprehensive information about a specific channel. The response includes both a flat structure with basic performance metrics (e.g., `win_rate_1d`) and a nested `analytics_by_timeframe` object with more advanced, calculated statistics.

## Request

<ParamField path="channelId" type="string" required>
  Unique channel identifier (e.g., "@adamowicztw")
</ParamField>

## Response

The endpoint returns a single [ChannelInfo](/api-reference/models/channel-models#channelinfo) object containing all available details for the channel.

## Example Request

<CodeGroup>
```bash cURL
curl -X GET "https://prod.api.sauron.outlight.fun/api/channels/@adamowicztw/info"
``````python Python
import requests

channel_id = "@adamowicztw"
response = requests.get(
    f"https://prod.api.sauron.outlight.fun/api/channels/{channel_id}/info"
)
data = response.json()
``````javascript JavaScript
const channelId = "@adamowicztw";
const response = await fetch(
  `https://prod.api.sauron.outlight.fun/api/channels/${channelId}/info`
);
const data = await response.json();
```
</CodeGroup>

## Example Response

*For a detailed breakdown of the response object structure, see the [ChannelInfo model](/api-reference/models/channel-models#channelinfo).*
```json
{
  "channel_id": "@adamowicztw",
  "username": "adamowicztw",
  "name": "Admados (Outlight.fun)",
  "platform": "twitter",
  "subscriber_count": 224,
  "calls_analyzed": 8,
  "total_calls_all": 8,
  "win_rate_all": 38,
  "avg_gain_all": 473,
  "x5_rate_all": 25,
  "x10_rate_all": 13,
  "unique_tokens": 8,
  "last_call_at": "2025-10-02T00:02:09.726Z",
  "first_call_at": "2025-08-31T16:00:20.744Z",
  "rds_score": null,
  "status": "pending",
  "analytics_by_timeframe": {
    "30d": {
      "time_to_ath_minutes": 42237.84,
      "survival_rate_percent": 100,
      "profit_risk_ratio": null,
      "average_gain_percent": 3061.13,
      "intelligent_tp": 3061.13,
      "intelligent_sl": -32.69,
      "rds_score": 100,
      "channel_style": "batch_ultra_fast",
      "calculated_at": "2025-10-22T20:24:14.539Z"
    }
  }
}
```

## Metrics Explained

The following table explains some of the key performance metrics returned in the `ChannelInfo` object.

| Metric | Description |
|---|---|
| `win_rate_all` | Percentage of all-time profitable calls. |
| `avg_gain_all` | Average percentage gain across all calls. |
| `x5_rate_all` | Percentage of calls achieving at least 5x returns. |
| `rds_score` | Risk-Diversification-Success score (0-100). A higher score is better. |
| `channel_style` | An algorithmic classification of the channel's calling behavior. |
| `survival_rate_percent` | Percentage of called tokens that maintained their value over a period. |
| `intelligent_sl` / `tp` | Suggested stop-loss and take-profit percentages based on historical data. |