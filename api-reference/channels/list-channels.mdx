---
title: 'List Channels'
api: 'GET /api/channels'
description: 'Get paginated list of channels with sorting options'
---

## Overview

Retrieve a paginated list of channels with advanced sorting options by various performance metrics.

## Request

<ParamField query="page" type="integer" default="1">
  Page number (starting from 1).
</ParamField>

<ParamField query="limit" type="integer" default="20">
  Number of results per page (1-200).
</ParamField>

<ParamField query="timeframe" type="string">
  Time window for metrics. Options: `1d`, `3d`, `7d`, `14d`, `30d`, `all`.
</ParamField>

<ParamField query="sort" type="string">
  Sort field. Options:
  - `win_rate` - Sort by win rate
  - `avg_gain` - Sort by average gain
  - `subscriber_count` - Sort by subscriber count
  - `total_calls` - Sort by number of calls
</ParamField>

<ParamField query="order" type="string" default="desc">
  Sort direction: `asc` or `desc`.
</ParamField>

## Response

<ResponseField name="channels" type="array">
  An array of [Channel](/api-reference/models/channel-models#channel) objects.
</ResponseField>

<ResponseField name="totalChannels" type="integer">
  Total number of channels available that match the criteria.
</ResponseField>

<ResponseField name="totalPages" type="integer">
  Total number of pages available.
</ResponseField>

<ResponseField name="currentPage" type="integer">
  The current page number.
</ResponseField>

## Example Request

<CodeGroup>
```bash cURL
curl -X GET "https://prod.api.sauron.outlight.fun/api/channels?page=1&limit=10&timeframe=30d&sort=win_rate&order=desc"
``````python Python
import requests

response = requests.get(
    "https://prod.api.sauron.outlight.fun/api/channels",
    params={
        "page": 1,
        "limit": 10,
        "timeframe": "30d",
        "sort": "win_rate",
        "order": "desc"
    }
)
data = response.json()
``````javascript JavaScript
const params = new URLSearchParams({
  page: 1,
  limit: 10,
  timeframe: '30d',
  sort: 'win_rate',
  order: 'desc'
});

const response = await fetch(
  `https://prod.api.sauron.outlight.fun/api/channels?${params}`
);
const data = await response.json();
```
</CodeGroup>

## Example Response

*For a detailed breakdown of the object structure in the `channels` array, see the [Channel model](/api-reference/models/channel-models#channel).*
```json
{
  "channels": [
    {
      "channel_id": "@TopCrypto",
      "username": "TopCrypto",
      "name": "Top Crypto Calls",
      "link": "https://t.me/TopCrypto",
      "subscriber_count": 250000,
      "profile_photo_url": "https://example.com/photo.jpg",
      "platform": "telegram",
      "calls_analyzed": 1250,
      "win_rate": "85.2",
      "avg_gain": "320.5",
      "total_calls_30d": "62",
      "win_rate_30d": "87.5",
      "avg_gain_30d": "385.2",
      "x5_rate": "22.5",
      "x10_rate": "12.3",
      "last_updated": "2025-01-15T12:00:00Z"
    }
  ],
  "totalChannels": 500,
  "totalPages": 50,
  "currentPage": 1
}
```

## Sorting Options

| Sort Field | Description |
|------------|-------------|
| `win_rate` | Percentage of successful calls |
| `avg_gain` | Average percentage gain across all calls |
| `subscriber_count` | Number of channel subscribers |
| `total_calls` | Total number of calls analyzed |

## Timeframe Options

| Timeframe | Description |
|-----------|-------------|
| `1d` | Last 24 hours |
| `3d` | Last 3 days |
| `7d` | Last 7 days |
| `14d`| Last 14 days |
| `30d`| Last 30 days |
| `all`| All-time metrics |